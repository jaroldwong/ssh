{
  "name": "student-schedule",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^3.1.1",
    "axios": "^0.21.1",
    "gh-pages": "^2.0.0",
    "react": "^16.8.6",
    "react-dom": "^16.5.2",
    "react-scripts": "2.1.8",
    "xlsx": "^0.14.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "sync": "aws s3 sync build/ s3://student-schedule"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
